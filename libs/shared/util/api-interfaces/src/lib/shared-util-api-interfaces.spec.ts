import { sharedUtilApiInterfaces } from './shared-util-api-interfaces';

describe('sharedUtilApiInterfaces', () => {
  it('should work', () => {
    expect(sharedUtilApiInterfaces()).toEqual('shared-util-api-interfaces');
  });
});
